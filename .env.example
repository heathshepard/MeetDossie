# MeetDossie environment variables — production values live in Vercel.
# Copy values to Vercel → Settings → Environment Variables.

# ── Frontend (safe to expose, must be prefixed VITE_) ─────────────────
VITE_SUPABASE_URL=https://pgwoitbdiyubjugwufhk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...

# ── Backend / serverless (server-only, NEVER prefix VITE_) ────────────
ANTHROPIC_API_KEY=sk-ant-api03-...
SUPABASE_URL=https://pgwoitbdiyubjugwufhk.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
ELEVENLABS_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
CRON_SECRET=

# ── Phase 1 marketing pipeline (NEW — set before deploying marketing crons) ──
TELEGRAM_BOT_TOKEN=          # @BotFather → /mybots → Claudy_Max_Bot → API Token
TELEGRAM_CHAT_ID=7874782923  # Heath's chat id with the bot (confirmed)
TELEGRAM_WEBHOOK_SECRET=     # any random 32+ char string; pass as secret_token to setWebhook
ZERNIO_API_KEY=              # required for cron-publish-approved
