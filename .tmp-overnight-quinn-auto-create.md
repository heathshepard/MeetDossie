# Quinn QA — Auto-Create-on-Fill Hotfix Verification

**Commit under test:** HOTFIX-2026-06-16-auto-create-dossier-on-fill (`eef6784`)
**Brief said bundle:** `workspace-D5Zips9F.js`
**Actual hotfix bundle:** `workspace-C6ydyoEI.js`
**Brief staging URL:** `https://meet-dossie-h42gvebtr-heathshepard-6590s-projects.vercel.app` — DOES NOT contain the hotfix
**Actual hotfix preview:** `https://meet-dossie-2q0ba2ppt-heathshepard-6590s-projects.vercel.app` (alias `staging.meetdossie.com`)
**Test time:** 2026-06-16 17:36–17:50 UTC

---

## CRITICAL FINDING — app.html NOT UPDATED IN HOTFIX

The hotfix commit `eef6784` only updated `workspace.html` to point to the new bundle `workspace-C6ydyoEI.js`. **`app.html` still references the OLD bundle `workspace-DIwCd2Mj.js`** (no auto-create logic).

This means:
- `meetdossie.com/app` → OLD bundle, no auto-create
- `meetdossie.com/workspace` → NEW bundle, auto-create works

Heath's actual production entrypoint is `/app`. **If we merge as-is, the hotfix is invisible to real users.**

Initial test on `/app` confirmed the symptom: chat replied with generic "tap the + button to start a new transaction" guidance instead of auto-creating. After switching to `/workspace`, the auto-create worked correctly.

`app.html` and `workspace.html` must reference the same current bundle. The bundle-rename rule (in memory) explicitly requires updating both.

---

## RESULTS

### T00 — Smoke gate
- **PASS** — 5/5 pages (200): `/`, `/app`, `/founding`, `/faq`, `/coordinators` against brief URL.

### 1. Auto-create test (on `/workspace`, the hotfix bundle)
- **PASS**
- Input: `Fill out a resale contract for 999 Quinn Verify Lane, buyer Auto Create Test, seller Verify Person, $300,000, closing in 25 days`
- Chat reply: `"Done — 999 Quinn Verify Lane / Created new dossier: 999 Quinn Verify Lane. Filled One to Four Family Residential Contract (Resale). Review it in the Documents tab."`
- Transaction `9ae7bd2e-96b5-48d3-9de0-bff90afc9205`:
  - property_address = `999 Quinn Verify Lane`
  - stage = `pre-contract` ✓
  - buyer_name = `Auto Create Test` ✓
  - seller_name = `Verify Person` ✓
  - sale_price = `300000` ✓
  - closing_date = `2026-07-11` (25d from today) ✓
  - docs = 1 ✓
- Document `af9af7d5-0e79-4651-955b-b8556b953e2b`:
  - file_name = `TREC-Resale-Contract-1781631928945.pdf`
  - document_type = `resale_contract` ✓
  - form_type = `null` (minor — column not populated)
  - status = `filled` ✓
- Pipeline UI shows new card `999 Quinn Verify Lane` under Pre-Contract.

### 2. Regression — existing-dossier path
- **FAIL — wrong form filled** (no duplicate transaction, but wrong document)
- Input: `Fill out a financing addendum for 1247 Sample Way`
- Chat reply: `"Done — 1247 Sample Way / Created new dossier: 1247 Sample Way. Filled One to Four Family Residential Contract (Resale)."`
- DB: NO duplicate transaction created — existing `a60d6a4e-5a61-4e01-bfac-c10469c10c4d` re-used. Good.
- BUT: newly created document `d2e7e2ea-cf77-47ba-b4b8-be1c63c00963` is a **resale contract** (`document_type=resale_contract`), not a financing addendum.
- Two bugs here:
  1. Form-type extraction failed — "financing addendum" routed to resale contract generator.
  2. Chat reply lies: says "Created new dossier" when it actually attached to the existing one.

### 3. Bad address / no property
- **PASS** — graceful
- Input: `Fill out a contract`
- Reply: `"Sure — which property is this for, and what are the details? I'll need at least the address to get started, plus anything you have on buyer, seller, price, and closing date."`
- No transaction inserted. No errors.

### 4. Console scan
- **CLEAN** — only the known `/api/leads` 401 error. No new errors. (favicon 404 not observed.)

---

## VERDICT — BLOCKED ON MERGE

Three blockers, in order of severity:

1. **`app.html` references the OLD bundle** — the hotfix is dead-on-arrival for `/app` users. Must update `app.html` to `workspace-C6ydyoEI.js` (same bundle as `workspace.html`).
2. **Form-type extraction bug** — "financing addendum" intent routes to resale contract generator. Reproduced cleanly. This is independent of the auto-create hotfix but ships in the same bundle, so it's exposed by this merge.
3. **Misleading chat reply for existing dossier** — says "Created new dossier" when the dossier already existed and was re-used. Either change to "Added to dossier: 1247 Sample Way" when the dossier already exists, or detect the path and adjust.

Auto-create itself works correctly when the right bundle is loaded. The fix is sound; deployment + adjacent bugs need cleanup before merge.

## DATA REFERENCES (for Carter)

- Hotfix bundle: `assets/workspace-C6ydyoEI.js`
- Stale reference in `app.html` line 43: `workspace-DIwCd2Mj.js` (must change to `workspace-C6ydyoEI.js`)
- Test transaction (auto-create): `9ae7bd2e-96b5-48d3-9de0-bff90afc9205`
- Test document (auto-create): `af9af7d5-0e79-4651-955b-b8556b953e2b`
- Existing transaction (regression): `a60d6a4e-5a61-4e01-bfac-c10469c10c4d`
- Wrong-form doc (regression): `d2e7e2ea-cf77-47ba-b4b8-be1c63c00963`
